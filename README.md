# Stock-Trading-Python
 A Python Stock Trading Script that lets you buy/sell stocks with Demo Money.

install the required modules by using "pip install -r requirements.txt"

or install it via the releases tab and execute the stocktrader.exe
